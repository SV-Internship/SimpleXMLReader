<UserControl x:Class="XmlReader.View.OpenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:XmlReader.View"
        mc:Ignorable="d"
        d:DesignHeight="40" d:DesignWidth="450">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Xml File Path" Margin="5,0"/>
            <TextBox IsReadOnly="True" Width="200" Text="{Binding XmlPath}"/>
            <Button Width="40" Content="..." Margin="5,0" Command="{Binding FindPath}"/>
            <Button Width="40" Content="ok" Margin="5,0" Command="{Binding OpenXml}"/>
        </StackPanel>
    </Grid>
</UserControl>
